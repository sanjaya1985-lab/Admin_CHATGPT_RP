window.UI={busy(b,t){if(!b)return;b.disabled=true;b.dataset.originalText=b.innerText;b.innerText=t||'Processing...'},ready(b,t){if(!b)return;b.disabled=false;b.innerText=t||b.dataset.originalText||'Submit'},text(id,v){const e=document.getElementById(id);if(e)e.textContent=v==null?'':String(v)},escape(v){const d=document.createElement('div');d.textContent=v==null?'':String(v);return d.innerHTML}};